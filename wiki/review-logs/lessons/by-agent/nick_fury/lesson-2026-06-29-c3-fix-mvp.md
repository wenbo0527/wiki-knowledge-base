# Lesson 2026-06-29: C-3 MVP 修复闭环（Fix-1 + Fix-2）

> 沉淀时间: 2026-06-29 10:35
> 触发 INC: INC-2026-06-29-001
> 维护: 尼克·弗瑞 🕵️
> 关联: lesson-2026-06-29-daily-9d-vacuum.md

---

## L-17: 监督层不能依赖"队列消费"假设（设计层 · 治本）

**踩坑**: C-3 脚本 6-18 设计时假设"Nick 唤醒时主动读 c3_alerts/ 推飞书"，但 9 天 0 消费侧。

**归属**: c3_daily_check.py 设计

**修复**: Fix-1 加 `push_to_feishu()` 函数（C-3 自带推送，不依赖外部消费侧）

**预防**:
- [ ] 任何"监督层"脚本必须自包含推送（写文件 + 推消息双通道）
- [ ] "队列消费"假设 = 反向依赖 = 反模式
- [ ] 告警文件只是"历史归档"，不是"通知手段"

**验证**: 未来 7 天 0 哑告警（C-3 任何告警必推飞书）

---

## L-18: 完稿判定用 OR 语义更鲁棒（判定层 · 治标）

**踩坑**: C-3 用 `## ✅ 完稿时间` 标记 + size > 2KB 严格 AND 判定，6-26/6-27 实质 6-8KB 完整内容被误判 0% 完稿率。

**归属**: c3_daily_check.py 判定逻辑

**修复**: Fix-2 改 OR 语义（任一条件即认为实质完稿）

**预防**:
- [ ] 完稿判定 = 流程语义（标记）OR 物理语义（size）任一即过
- [ ] 严格 AND 容易误杀，OR 更鲁棒
- [ ] 但要警惕：size 略过阈值的骨架也会被认完稿（6-25 2229B 案例）

**验证**: 7 天 daily 完稿率识别 ≥ 80%（与人工判断一致）

**反例**: 6-25 (2229B) 现在被认完稿——可能争议，等文博拍板是否提高阈值到 3000B

---

## L-19: dry-run 验证后再 kickstart（执行层 · 流程治本）

**踩坑**: 如果直接 kickstart 而不 dry-run，可能导致 plist 触发失败但难以诊断。

**修复**: 标准流程 `bootout + bootstrap + kickstart -k + 验证 status=0`

**预防**:
- [ ] 改 plist 调用脚本前必须 `python3 <script> dry-run` 验证
- [ ] `launchctl kickstart -k` 强制重跑（不等下一个 cron 周期）
- [ ] `launchctl list | grep <label>` 验证 status=0（=就绪）

**验证**: 6-29 10:31 完整跑通：syntax OK → dry-run 80% → bootout → bootstrap → kickstart → status=0

---

## L-20: 用户 scope 缺失要早发现（系统层 · 工具链治本）

**踩坑**: dry-run 走"无告警"路径，**没触发** push_to_feishu → 飞书 user scope 缺失**没被暴露**。下次 21:00 真告警会失败。

**归属**: lark-cli 工具链 + Nick 自身

**修复**: 待办 — 升级 lark-cli 1.0.42 → 1.0.59 + scope 授权

**预防**:
- [ ] 任何推送功能上线前必须**模拟真实触发**（不只是写 dry-run）
- [ ] 集成测试要触发"实际副作用"，不只是"主流程跑通"
- [ ] scope 阻塞要进 lessons 库（已加 L-20）

**验证**: 21:00 真告警时 push_to_feishu 成功（lark-cli 升级 + scope 授权后）

---

## 与已有教训的关系

| 新教训 | 关联历史 | 关系 |
|:---|:---|:---|
| L-17 | L-13（任务真空自检必须可见）| 升级版：从"文件可见"升级到"推送可见"|
| L-18 | L-9（API 分页 has_more 不可信）| 同源：单一信号易误判，多信号 OR 更稳 |
| L-19 | L-6（修复前先 dry-run）| 复用：dry-run 已经是标准动作 |
| L-20 | L-7（修一个 ≠ 修一类）| 升级版：发现 lark-cli scope 通用问题 |

---

## 下次 24h 验证 checklist

- [ ] 6-29 21:00 触发，c3_daily.log 应追加 ratio ≥ 80% OK 记录
- [ ] 6-29 21:00 触发，data/c3_alerts/ 不应新增（除非有真告警）
- [ ] 6-30 09:00 触发，连续 2 次 OK
- [ ] 6-30 09:00 文博收到 webchat 提示"日报连续 2 天 OK"
- [ ] 7 天 0 哑告警 → INC-001 标 Closed

---

🕵️ 尼克·弗瑞
2026-06-29 10:35
*4 条新教训 · 1 设计 + 1 判定 + 1 流程 + 1 工具链 · MVP 闭环*
